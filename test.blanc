fnc fact(x) {
    let i = 1;
    let u = 1;
    while i <= x {
        let y = x;
        u *= i;
        i += 1;
    };
    print(y);
    return u;
};

5.fact().print();
print("this is line", line(), "column", column(), "file", file());

fnc sum(arr) {
    let i = 0;
    for y in arr {
        i += y;
    };
    return i;
};

sum([1,2,3,4,5,6,7,8,9]).print();
